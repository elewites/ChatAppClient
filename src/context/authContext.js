import { createContext } from "react";

//generating authentication context
//used to store global states
//Explore App.js, in there AuthContext wraps all routes of the app
export const AuthContext = createContext({});
